# 🏗️ ARCHITECTURE DIAGRAM

## System Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                          USER BROWSER                            │
│                     (Chrome, Firefox, etc.)                      │
└────────────┬────────────────────────────────────┬────────────────┘
             │                                    │
             ▼                                    ▼
    ┌─────────────────┐                 ┌─────────────────┐
    │  index.html     │                 │  encode.html    │
    │  (Home Page)    │                 │  decode.html    │
    │  + style.css    │◄────────────────┤  + encode.js    │
    │                 │    Navigation   │  + decode.js    │
    └─────────────────┘                 └────────┬────────┘
                                                 │
                                                 │ HTTP Requests
                                                 │ (multipart/form-data)
                                                 ▼
┌─────────────────────────────────────────────────────────────────┐
│                      NODE.JS SERVER                              │
│                       (server.js)                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Express Framework + Multer (File Uploads)                      │
│                                                                  │
│  API Endpoints:                                                  │
│  ┌──────────────────────────────────────────────────────┐      │
│  │  POST /api/encode                                     │      │
│  │    - Receives: coverImage + secretFile                │      │
│  │    - Calls: stego.exe encode ...                      │      │
│  │    - Returns: Download link                           │      │
│  └──────────────────────────────────────────────────────┘      │
│                                                                  │
│  ┌──────────────────────────────────────────────────────┐      │
│  │  POST /api/decode                                     │      │
│  │    - Receives: stegoImage                             │      │
│  │    - Calls: stego.exe decode ...                      │      │
│  │    - Returns: Download link                           │      │
│  └──────────────────────────────────────────────────────┘      │
│                                                                  │
│  ┌──────────────────────────────────────────────────────┐      │
│  │  GET /api/download/:filename                          │      │
│  │    - Serves file from output/ directory               │      │
│  └──────────────────────────────────────────────────────┘      │
│                                                                  │
└────────────┬─────────────────────────────────┬─────────────────┘
             │                                 │
             │ Process Execution               │ File I/O
             │ (child_process.exec)            │
             ▼                                 ▼
    ┌──────────────────┐              ┌──────────────────┐
    │  stego.exe       │              │  File System     │
    │  (C++ Program)   │◄─────────────┤  uploads/        │
    │                  │   Read/Write │  output/         │
    └────────┬─────────┘              └──────────────────┘
             │
             │ Command-line Args
             │
    ┌────────▼──────────────────────────────────────────────┐
    │                                                        │
    │  STEGANOGRAPHY ENGINE (stego_cli.cpp)                 │
    │                                                        │
    │  ┌──────────────────────────────────────────────┐    │
    │  │  ENCODE MODE                                  │    │
    │  │  stego.exe encode <cover> <secret> <output>  │    │
    │  │                                               │    │
    │  │  1. Read cover image                         │    │
    │  │  2. Read secret file                         │    │
    │  │  3. Validate sizes (secret ≤ 85% of cover)  │    │
    │  │  4. Create header with metadata              │    │
    │  │  5. Append: [Cover] + [Header] + [Secret]   │    │
    │  │  6. Write stego image                        │    │
    │  └──────────────────────────────────────────────┘    │
    │                                                        │
    │  ┌──────────────────────────────────────────────┐    │
    │  │  DECODE MODE                                  │    │
    │  │  stego.exe decode <stego> <output>           │    │
    │  │                                               │    │
    │  │  1. Read stego image                         │    │
    │  │  2. Search for magic signature (0x5354454E)  │    │
    │  │  3. Extract and validate header              │    │
    │  │  4. Read hidden file data                    │    │
    │  │  5. Write extracted file                     │    │
    │  └──────────────────────────────────────────────┘    │
    │                                                        │
    └────────────────────────────────────────────────────────┘
```

---

## Data Structure - Stego File Format

```
┌───────────────────────────────────────────────────────────────┐
│                        STEGO FILE                             │
├───────────────────────────────────────────────────────────────┤
│                                                               │
│  ╔═══════════════════════════════════════════════════════╗  │
│  ║           ORIGINAL COVER IMAGE DATA                   ║  │
│  ║           (Original file content preserved)           ║  │
│  ║           Size: Variable (10 KB minimum)              ║  │
│  ╚═══════════════════════════════════════════════════════╝  │
│                                                               │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │  HEADER (Metadata)                                      │ │
│  ├─────────────────────────────────────────────────────────┤ │
│  │  Magic Signature:     0x5354454E ("STEN")  [4 bytes]   │ │
│  │  Version:             0x0001                [2 bytes]   │ │
│  │  Hidden File Size:    <size in bytes>       [4 bytes]   │ │
│  │  Filename Length:     <length>              [2 bytes]   │ │
│  │  Original Filename:   <name>                [256 bytes] │ │
│  │  Checksum:            <integrity check>     [4 bytes]   │ │
│  └─────────────────────────────────────────────────────────┘ │
│                                                               │
│  ╔═══════════════════════════════════════════════════════╗  │
│  ║           HIDDEN FILE DATA                            ║  │
│  ║           (Secret file content)                       ║  │
│  ║           Size: ≤ 85% of cover image                  ║  │
│  ╚═══════════════════════════════════════════════════════╝  │
│                                                               │
└───────────────────────────────────────────────────────────────┘
```

---

## File Flow Diagram

### ENCODING FLOW:

```
 User Selects Files
         ↓
 ┌───────────────────┐
 │ cover.png (500KB) │
 │ secret.txt (50KB) │
 └────────┬──────────┘
          ↓
    Browser Upload
          ↓
    [encode.js]
          ↓
    POST /api/encode
          ↓
    [server.js]
          ↓
   Save to uploads/
          ↓
    Execute Command:
    stego.exe encode
      uploads/cover.png
      uploads/secret.txt
      output/stego-123.png
          ↓
    [stego_cli.cpp]
     ├─ Read cover.png ──────► [500 KB]
     ├─ Read secret.txt ─────► [50 KB]
     ├─ Create header ───────► [~300 bytes]
     ├─ Validate size ───────► ✓ (50KB < 425KB)
     └─ Combine files ───────► [~550 KB total]
          ↓
    Write output/stego-123.png
          ↓
    Return success to server
          ↓
    [server.js]
     └─ Send download link
          ↓
    [encode.js]
     └─ Show download button
          ↓
    User Downloads stego-123.png
```

### DECODING FLOW:

```
 User Uploads Stego Image
         ↓
 ┌────────────────────┐
 │ stego-123.png      │
 │ (550 KB)           │
 └────────┬───────────┘
          ↓
    Browser Upload
          ↓
    [decode.js]
          ↓
    POST /api/decode
          ↓
    [server.js]
          ↓
   Save to uploads/
          ↓
    Execute Command:
    stego.exe decode
      uploads/stego-123.png
      output/extracted-456.bin
          ↓
    [stego_cli.cpp]
     ├─ Read stego-123.png ──────► [550 KB]
     ├─ Search for signature ────► Found at offset X
     ├─ Extract header ──────────► Get metadata
     ├─ Validate checksum ───────► ✓ Valid
     ├─ Original name: secret.txt
     ├─ Extract hidden data ─────► [50 KB]
     └─ Write to output ─────────► extracted-456.bin
          ↓
    Rename to original: secret.txt
          ↓
    Return success to server
          ↓
    [server.js]
     └─ Send download link
          ↓
    [decode.js]
     └─ Show download button
          ↓
    User Downloads secret.txt
```

---

## Technology Stack

```
┌─────────────────────────────────────────────────┐
│                 FRONTEND                        │
├─────────────────────────────────────────────────┤
│  • HTML5 (Structure)                            │
│  • CSS3 (Modern UI with gradients & animations) │
│  • Vanilla JavaScript (AJAX, DOM manipulation)  │
│  • Fetch API (HTTP requests)                    │
└─────────────────────────────────────────────────┘
                      ↕
┌─────────────────────────────────────────────────┐
│                 BACKEND                         │
├─────────────────────────────────────────────────┤
│  • Node.js (Runtime)                            │
│  • Express.js 4.18+ (Web framework)             │
│  • Multer 1.4+ (File upload middleware)         │
│  • child_process.exec (Process execution)       │
└─────────────────────────────────────────────────┘
                      ↕
┌─────────────────────────────────────────────────┐
│               CORE ENGINE                       │
├─────────────────────────────────────────────────┤
│  • C++ (ISO C++11)                              │
│  • STL (vector, string, iostream, fstream)      │
│  • Binary file I/O                              │
│  • Custom steganography algorithm               │
└─────────────────────────────────────────────────┘
```

---

## Component Interactions

```
┌──────────┐  HTTP    ┌──────────┐  exec()   ┌──────────┐
│          │ Request  │          │  Process  │          │
│ Frontend ├─────────►│ Backend  ├──────────►│   C++    │
│  (HTML/  │          │ (Node.js)│           │ Program  │
│   JS)    │◄─────────┤          │◄──────────┤          │
│          │ Response │          │  Result   │          │
└──────────┘          └────┬─────┘           └────┬─────┘
                           │                      │
                           │                      │
                           ▼                      ▼
                    ┌─────────────┐      ┌──────────────┐
                    │   uploads/  │      │   output/    │
                    │  (temp files│◄─────┤ (result files│
                    │   storage)  │      │   storage)   │
                    └─────────────┘      └──────────────┘
```

---

## Security & Validation Layers

```
┌─────────────────────────────────────────────────────┐
│ Layer 1: Frontend Validation                        │
│  • File type checking (accept attribute)            │
│  • Basic size display                               │
└────────────────┬────────────────────────────────────┘
                 ↓
┌─────────────────────────────────────────────────────┐
│ Layer 2: Server Validation (server.js)             │
│  • File size limits (50 MB max)                     │
│  • Required files check                             │
│  • Error handling                                   │
└────────────────┬────────────────────────────────────┘
                 ↓
┌─────────────────────────────────────────────────────┐
│ Layer 3: C++ Validation (stego_cli.cpp)            │
│  • File existence check                             │
│  • Minimum host size (10 KB)                        │
│  • Maximum hidden size (85% of host)                │
│  • Magic signature verification                     │
│  • Checksum validation                              │
│  • Header integrity check                           │
└─────────────────────────────────────────────────────┘
```

---

**This diagram shows the complete architecture and data flow of the StegoProto application.**
